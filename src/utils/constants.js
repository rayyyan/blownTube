import { Dimensions } from "react-native"

const { width, height } = Dimensions.get("window")
export default {
  colors: {
    black: "#0F0D13",
    white: "#fff",
    purple: "#654AFF",
    lightBlack: "#17151B",
  },
  width,
  height,
}
